export interface Dish {
  id: number;
  name: string;
  nameThai: string;
  price: number;
  description: string;
  ingredients: string;
  image: string;
}

export interface Beverage {
  name: string;
  price: number;
}